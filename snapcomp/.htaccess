Test

# Prevent Apache from serving .htaccess files:
<FilesMatch "^\.htaccess">
    Order allow,deny
    Deny from all
</FilesMatch>
RewriteEngine on
RewriteCond %{HTTPS} !=on
RewriteRule (.*) https://localhost/tmp/snapcomp [R]
